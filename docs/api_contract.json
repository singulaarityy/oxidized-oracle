{
  "description": "Format data yang dikirim ESP32 ke Rust Backend",
  "method": "POST",
  "endpoint": "/api/submit",
  "payload": {
    "device_id": "String (ex: 'sensor_alpha')",
    "timestamp": "Integer (Unix Epoch)",
    "data_hash": "String (SHA256 hex)",
    "signature": "String (Ed25519 hex signature)"
  }
}